import{createHmac,timingSafeEqual}from"node:crypto";export class ResumeTokens{constructor(private secret:string){if(secret.length<24)throw new Error("RESUME_SECRET must be at least 24 characters") }issue(episodeId:string){const sig=createHmac("sha256",this.secret).update(episodeId).digest("base64url");return`${episodeId}.${sig}`}verify(token:string){const [id,sig]=token.split(".");if(!id||!sig)return null;const expected=createHmac("sha256",this.secret).update(id).digest("base64url");const a=Buffer.from(sig),b=Buffer.from(expected);return a.length===b.length&&timingSafeEqual(a,b)?id:null}}
