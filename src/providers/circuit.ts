import type{ProviderContext,SearchProvider}from"./base.js";export class CircuitProvider implements SearchProvider{private failures=0;private openedAt=0;name:string;constructor(private inner:SearchProvider,private threshold=3,private cooldownMs=30_000){this.name=inner.name}enabled(){return this.inner.enabled()&&(this.openedAt===0||Date.now()-this.openedAt>=this.cooldownMs)}async search(c:ProviderContext){if(!this.enabled())throw new Error(`${this.name} circuit open`);try{const r=await this.inner.search(c);this.failures=0;this.openedAt=0;return r}catch(e){this.failures++;if(this.failures>=this.threshold)this.openedAt=Date.now();throw e}}}
