# divAIne Search web app

Static, zero-build frontend for Vercel. Set the Vercel project's Root Directory to `frontend`. The production custom domain is `search.divaine.io`. API and hosted MCP remain on the Render backend.
