import{McpServer}from"@modelcontextprotocol/sdk/server/mcp.js";import{StdioServerTransport}from"@modelcontextprotocol/sdk/server/stdio.js";import{z}from"zod";import type{SearchHarness}from"./core/harness.js";import{SearchRequestSchema}from"./contracts/search.js";export async function runMcp(h:SearchHarness){const m=new McpServer({name:"divaine-search",version:"0.2.0"});m.tool("personalized_web_search","Write a mandate, route across search supply, and return ranked source records",{query:z.string(),tenant_id:z.string(),user_id:z.string().optional(),session_id:z.string().optional(),context:z.array(z.object({key:z.string(),value:z.any(),source:z.enum(["query","caller","human","prior_outcome"]),confidence:z.number().optional()})).optional(),may_ask_user:z.boolean().optional(),may_retain:z.boolean().optional(),max_results:z.number().optional()},async x=>{const r=await h.search(SearchRequestSchema.parse({query:x.query,tenant_id:x.tenant_id,user_id:x.user_id,session_id:x.session_id,context:x.context??[],permissions:{may_pull_context:true,may_ask_user:x.may_ask_user??false,may_retain:x.may_retain??false,may_learn:false,scopes:[]},limits:{max_results:x.max_results??10}}));return{content:[{type:"text",text:JSON.stringify(r,null,2)}],structuredContent:r as any}});await m.connect(new StdioServerTransport())}
