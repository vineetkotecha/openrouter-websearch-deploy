import{describe,it,expect}from"vitest";import{buildQueryPlan,exaParams,serpApiParams,valyuParams,firecrawlParams,linkupParams}from"../src/providers/query-plan.js";
const req:any={query:"find a laptop available near me today",tenant_id:"t",locale:"en-IN",country:"IN",hard_constraints:{location:"Bengaluru",include_domains:["example.com"],exclude_domains:["spam.test"],freshness:"day"},context:[],permissions:{},limits:{max_results:10}};
const mandate:any={intent:req.query,category:"shopping",factors:[{key:"budget",class:"functional",value:1000,hard:true}],gaps:[]};
describe("provider query plan",()=>{const p=buildQueryPlan(req,mandate,new Date("2026-09-22T00:00:00Z"));it("derives shared semantics",()=>{expect(p.task_class).toBe("local");expect(p.vertical.engine).toBe("shopping");expect(p.locale).toEqual({language:"en",country:"in",location:"Bengaluru"});expect(p.source_policy.include_domains).toEqual(["example.com"]);expect(p.freshness.relative).toBe("day")});it("maps shopping and locale for SerpApi",()=>expect(serpApiParams(p)).toMatchObject({engine:"google_shopping",gl:"in",hl:"en",location:"Bengaluru",tbs:"qdr:d"}));it("maps domains and date for Exa",()=>expect(exaParams(p)).toMatchObject({includeDomains:["example.com"],excludeDomains:["spam.test"],startPublishedDate:"2026-09-21"}));it("maps Firecrawl location and time",()=>expect(firecrawlParams(p)).toMatchObject({location:"Bengaluru",tbs:"qdr:d"}));it("maps Linkup dates/domains",()=>expect(linkupParams(p)).toMatchObject({includeDomains:["example.com"],excludeDomains:["spam.test"],fromDate:"2026-09-21"}));it("uses proprietary Valyu mode for finance",()=>{const f={...p,vertical:{engine:"finance",proprietary_sources:["valyu/valyu-sec-filings"]}} as any;expect(valyuParams(f)).toMatchObject({search_type:"proprietary",included_sources:["valyu/valyu-sec-filings"]})})});
